
@using SportShop.Models;
<div>
    @foreach (Clothe item in Model)
    {
        <div>
            @item.model
            <img src=@item.photo>
            @item.quantity
        </div>
    }
</div>

